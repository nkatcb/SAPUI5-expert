<mvc:View xmlns="sap.m" xmlns:mvc="sap.ui.core.mvc" controllerName="dtt.project1.controller.InvoicesList">
    <Table id="_IDGenList1" headerText="{i18n>invoiceListTitle}" class="sapUiResponsiveMargin" width="auto" items="{northwind>/Invoices}">
        <headerToolbar>
            <Toolbar id="_IDGenToolbar1">
                <Title id="_IDGenTitle1" text="{i18n>invoiceListTitle}" />
                <ToolbarSpacer id="_IDGenToolbarSpacer1" />
                <SearchField id="_IDGenSearchField1" width="50%" search=".onFilterInvoices" />
            </Toolbar>
        </headerToolbar>
        <columns>
            <Column hAlign="End" minScreenWidth="Small" demandPopin="true" width="5em">
                <Text text="{i18n>columnQuantity}" />
            </Column>
            <Column>
                <Text text="{i18n>columnName}" />
            </Column>
          <!--   <Column minScreenWidth="Small" demandPopin="true">
                <Text text="{i18n>columnStatus}" />
            </Column> -->
            <Column minScreenWidth="Small">
                <Text text="{i18n>columnSupplier}" />
            </Column>
            <Column hAlign="End">
                <Text text="{i18n>columnPrice}" />
            </Column>
        </columns>

        <items>
            <ColumnListItem type="Navigation" press=".navigateToDetails">
                <cells>
                    <ObjectNumber number="{northwind>Quantity}" emphasized="false" />
                    <ObjectIdentifier title="{northwind>ProductName}" />
                    <!-- <Text id="_IDGenObjectStatus1" text="{path:'northwind>Status', 
                        formatter:'.formatter.invoiceStatus'}" /> -->
                    <Text text="{northwind>ShipperName}" />
                    <ObjectNumber
                        id="_IDGenObjectListItem1"
                        title="{northwind>Quantity}x{northwind>ProductName}"
                        number="{parts:[{path:'northwind>ExtendedPrice'}, {path:'Currency>/usd'}],
                                                            type: 'sap.ui.model.type.Currency', formatOptions:{showMeasure:false}}"
                        numberUnit="{Currency>/usd}"
                        numberState="{=${northwind>ExtendedPrice}>50 ? 'Success' : 'Error'}"
                    />
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</mvc:View>
